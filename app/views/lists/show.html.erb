<h1>Vista de lista de tareas</h1>

<p>Nombre: <%= @list.name %></p>

<h3>Items</h3>

<ul>
    <li><%= link_to 'añadir item', new_list_item_path(@list.id) %></li>
    <% @list.items.each do |item|%>
        <li><%=item.name%> | <%item.done ? 'lista' : 'en proceso' %><%= link_to "X", list_item_path(@list.id, item.id), method: :patch%></li>
    <%end%>
</ul>